# Model Provider Selection
# Choose between 'lm_studio' (local) or 'gemini' (Google API)
# Default: lm_studio
MODEL_PROVIDER=lm_studio

# LM Studio Configuration (for local models)
# Base URL for LM Studio server (default: http://localhost:1234/v1)
LM_STUDIO_BASE_URL=http://127.0.0.1:1234

# Model name to use (should match the model loaded in LM Studio)
LM_STUDIO_MODEL=local-model

# Optional: API key for LM Studio (not required, but can be set)
# LM_STUDIO_API_KEY=lm-studio

# Gemini API Configuration (for Google Gemini)
# Get your API key from: https://makersuite.google.com/app/apikey
# GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Gemini model name (default: models/gemini-flash-lite-latest)
# GEMINI_MODEL=models/gemini-flash-lite-latest

# Optional: Maximum output tokens for Gemini (default: 32768)
# Increase this for longer, more comprehensive responses
# GEMINI_MAX_OUTPUT_TOKENS=32768

# Reranker Configuration
# Enable neural reranker using cross-encoder model (default: true)
# Set to 'false' to use rule-based reranking
USE_NEURAL_RERANKER=true

# Optional: Reranker model name (default: cross-encoder/ms-marco-MiniLM-L-6-v2)
# Other options: cross-encoder/ms-marco-MiniLM-L-12-v2 (more accurate but slower)
# RERANKER_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2

# Rolling Window Configuration (prevents memory overflow)
# Maximum number of messages to keep in conversation history (default: 20)
# MAX_HISTORY_MESSAGES=20

# Maximum total characters in conversation history (default: 8000)
# Older messages are removed when limit is exceeded
# MAX_HISTORY_CHARS=8000

# Maximum characters per message in history (default: 2000)
# Longer messages are truncated
# MAX_MESSAGE_CHARS=2000

# Maximum total characters in document context (default: 12000)
# Documents are truncated or removed when limit is exceeded
# MAX_CONTEXT_CHARS=12000

# Maximum characters per document in context (default: 2000)
# MAX_DOC_CHARS=2000

# Optional: ChromaDB persistence directory
# CHROMA_PERSIST_DIRECTORY=./chroma_db

# Optional: Logging level
# LOG_LEVEL=INFO
